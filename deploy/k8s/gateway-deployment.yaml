apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  namespace: skincare-ai
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      containers:
        - name: gateway
          image: your-dockerhub-user/gateway:latest
          ports:
            - containerPort: 8000
